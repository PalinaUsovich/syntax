Encapsulation:
1)Is a mechanism in Java for wrapping the data (Variable) and
code acting on the data(methods) together as a single unit.

class Person{
private accountBalance;
private password;
String securityQuestion;
public accessAccountBalance(String password){
if(this.password.equal(password)){
sout"your account balance" + accountBalance
}
public void setAccountBalance(String password, String securityQuestion, double balance){
if(this.password.equals(password) && this.secirutyQuestion.equals(securityQuestion)){
this.accountBalance = accountBalance;
}
}
}