package constructors;

public class Task4 {
      Task4 (){ //static field they are initialize in the memory before we create an object
                      //only one value , only one memory location ,no more than just 1 object
    }
}
